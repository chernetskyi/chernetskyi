[user]
    name = Volodymyr Chernetskyi
    email = v.v.chernetskyi@gmail.com
    signingkey = B3C1E9705559E743
[core]
    editor = nvim
[commit]
    gpgsign = true
[init]
    defaultBranch = main
[alias]
    a   = add
    b   = branch
    c   = commit --allow-empty
    d   = diff
    f   = fetch
    i   = init
    l   = log --decorate --graph --format='%Cred%h %Cgreen%cn %Cblue%cr %Creset%s'
    m   = merge --squash
    r   = rebase
    s   = status --short
    t   = tag
    bl  = blame
    cf  = config
    ch  = checkout
    cl  = clone
    cp  = cherry-pick
    dl  = clone --depth 1 --single-branch --branch
    pl  = pull
    ps  = push
    rb  = rebase
    rl  = reflog
    rm  = rm -r
    rv  = revert
    sb  = clone --single-branch --branch
    sl  = shortlog
    st  = stash
    sw  = switch
    rmt = remote

